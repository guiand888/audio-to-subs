/// <reference types="vite/client" />

// Injected by Vite's `define` (see vite.config.ts / vitest.config.ts).
// The app version baked into the bundle at build time.
declare const __APP_VERSION__: string
